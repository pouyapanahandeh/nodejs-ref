/*
    Data types in js: undefined,null, boolean, string, symbol, number and object.
*/

var type = "pooya" // define string variable

type = 8

let typeTwo = "panahandeh" // another way to define variable

const typeThree = 3.14 // as you can see we change the type value, but we can not change the const value.
                      // if we change const value we will get errors.
