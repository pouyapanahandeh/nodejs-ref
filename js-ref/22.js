// write number in different way

function sum(a, b) {
	return a + b;
}

// if you wanna use ${} you need to put it with ``

alert(`result ${sum(1,2)}`);


// if we use `` we can span  string to multile lines

let guestList = `guests:
	*pooya
	*mohammad
	*parvaneh
	*masih`;
	
document.write(guestList);

