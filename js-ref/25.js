// loop in Array()

let life = ["family", "happines", "trust", "change"]

for (i = 0 ; i < life.length ; i++) {
	alert(life[i])
}

// short version of for loop

let fruits = ["Apple", "Orange", "Plum"];

// iterates over array elements for..of
for (let fruit of fruits) {
	alert( fruit );
}